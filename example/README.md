# pulumi-reolink example

This project has no dependencies of its own — it relies on `pulumi_reolink`,
`pulumi`, and `pyyaml` already being installed. Run `pulumi` commands here from
within the repo root's `nix-shell` (see the main [README](../README.md)),
which installs and activates all three automatically.
